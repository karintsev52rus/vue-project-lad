export enum Links {
  HOME = '/',
  SEARCH = '/search',
  ADVERT = '/advert',
  LOGIN = '/login',
  NEW = '/new'
}

export enum PathNames {
  HOME = 'home',
  ADVERT = 'advert',
  SEARCH = 'search',
  LOGIN = 'login',
  NEW = 'new'
}
